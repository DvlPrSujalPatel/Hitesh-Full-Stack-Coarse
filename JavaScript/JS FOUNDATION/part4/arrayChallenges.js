// 1. Declare an array name "teaFlavors" that contains the strings "green tea", "black tea", and "herbal tea".
    // Access the first item in the array
    // Access the last item in the array
    // Access the second item in the array
    // Access the second-to-last item in the array
    // Access the last-but-one item in the array
    // Access the 3rd item in the array store it as favourite tea


    let teaFlavors = ["green tea", "black tea", "herbal tea"];

    // console.log(teaFlavors[0]);
    // console.log(teaFlavors[2]);
    // console.log(teaFlavors[1]);
    // console.log(teaFlavors[1]);
    // console.log(teaFlavors[2]);
    let favoriteTea = teaFlavors[2];
    // console.log(favoriteTea);

// 1. Declare an array name `cities` containing the strings `'New York'`, `'London'`, and `'Paris'`.
    // Access the 3rd element in the array and store it
    let cities = ["New York", "London", "Paris"];
    favoriteCity = cities[2];
    // console.log(favoriteCity)

// 3. You have an Array name "TeaTypes" containing "herbal tea", "black tea", and "green tea" Change the